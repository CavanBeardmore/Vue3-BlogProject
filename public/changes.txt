CHANGES TO BE MADE:

posts: {
edit button (only see these is created the post or if admin)
toggling the view of search, view posts create posst needs to change to a universal toggle {
    have a ref for create, search, own posts all set to false,
    have a ref for posts set to true, 
    have buttons for e
}

}


bugs - need else clause when deleting tags or it delets the last entry - fixed

a main function will use helper functions and provide a true or false value
        a helper function will need to check for identical title and or content
                can add multiple identical posts??

not notifying when a post does not meet requirements only when published
